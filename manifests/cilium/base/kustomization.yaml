namespace: cilium-system
resources:
  - namespace.yaml
  - high-priority-scheduling.yaml
  - bgp-config.yaml

helmCharts:
  - name: cilium
    repo: https://helm.cilium.io/
    version: 1.14.2
    releaseName: cilium
    namespace: cilium-system
    includeCRDs: true
    valuesFile: values.yaml
